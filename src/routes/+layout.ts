// Enable static prerendering for the whole app (required by adapter-static
// when deploying to GitHub Pages, which serves only static files).
export const prerender = true;
export const csr = true;
export const trailingSlash = 'never';